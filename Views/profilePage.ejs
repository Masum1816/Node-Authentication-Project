<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
    <link href="Style/main.css" rel="stylesheet">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

    <style>
        .background {
            width: 100%;
            height: 1000px;
            background-color: bisque;
        }
        .text {
            text-align: center;
            font-family: fantasy;
        }
        .userName {
            padding-top: 40px;
        }
    </style>
</head>
<body>
    <!-- <div class="background">
        <% if (user) { %>
            <div class="user-card">
                <div class="user-card-content">
                    <h2 class="user-name text userName">Name: <%= user.name %></h2>
                    <h2 class="user-email text">Email: <%= user.email %></h2>
                </div>
            </div>
            <a href="/changePassword">Change Password</a>
        <% } else { %>
            <p class="text userName">No user found</p>
        <% } %>
    </div> -->

    <div class="container">
        <div class="row">
            <div class="col-4"></div>
            <div class="form-container-profile mt-5">
                <h3 class="form-btn-profile w-100 mt-3 mb-2 text-center">Profile</h3>
                <% if (user) { %>
                    <div class="user-card">
                        <div class="user-card-content">
                            <h3 class="input w-100 mt-3">Name: <%= user.name %></h3>
                            <h3 class="input w-100 mt-3">Email: <%= user.email %></h3>
                        </div>
                    </div>
                    <a href="/changePassword" class="sign-up-link">Change Password</a>
                <% } else { %>
                    <p class="text userName">No user found</p>
                <% } %>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
